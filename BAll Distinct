/* Yildiz ☆ */
using namespace std;
#include <bits/stdc++.h>
#define habboosh main
#define heba ios::sync_with_stdio(false); cin.tie(nullptr);
#define loop   for (int i = 1; i <t.length(); ++i) {
#define ll long long
#define w   while (t--)
#define all(x) (x).begin(), (x).end()
int habboosh() {
    heba
    int t;
    cin>>t;
   w
    {
        int n;  set<int> s;
        cin>>n;
        int a[n];
        for(int i=0; i<n; i++)
        {
            cin>>a[i];
            s.insert(a[i]);
        }
        int l = s.size();
        int r;
 
        if ((n - l) % 2 == 0) {
            r = l;
        } else {
            r = l - 1;
        }
 
        cout << r << "\n";
    }
    return 0;
